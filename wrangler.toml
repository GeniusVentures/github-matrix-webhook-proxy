name = "github-matrix-webhook-proxy"
main = "src/index.ts"
compatibility_date = "2026-07-17"

# Keep this repository deployment-neutral. Configure routes, room IDs,
# organization policy, and WEBHOOK_FILTER_CONFIG in a private deployment
# repository or through Cloudflare environment bindings.
keep_vars = true
